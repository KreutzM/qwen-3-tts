## What changed

Describe the smallest useful change.

## Verification

- [ ] Relevant unit tests pass
- [ ] `scripts/doctor.sh` remains meaningful for environment changes
- [ ] No model weights, generated audio, private voice samples or secrets are committed
- [ ] Documentation reflects changed setup/commands

For GPU/model changes, include measured evidence (GPU, model ID, attention backend, success/failure, and relevant VRAM/timing data) rather than only stating that it works.

## Notes / remaining uncertainty

State what was not tested, especially model downloads, GPU-specific behavior or long-form audio quality.
